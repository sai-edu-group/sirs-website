---
import Layout from "@/layouts/Layout.astro";
import DummySection from "@/components/ui/DummySection.astro";

const { slug } = Astro.params;
---

<Layout meta={{ title: "Campus" }}>
  <DummySection name={`Campus — ${slug}`} />
</Layout>
